apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "PingPong"
version = '1.0'
mainClassName = "PingPong"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
